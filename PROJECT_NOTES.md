# 곤충 배틀 도감 — 프로젝트 개발 노트

> 이 문서는 사용자용 안내(README.md)와 별개로, **나중에 이 프로젝트를 다시
> 열어 수정할 때** 빠르게 맥락을 파악하기 위한 내부 참고 문서예요.
> "왜 이렇게 만들었는지", "숫자를 어디서 바꾸는지"를 중심으로 적어뒀어요.

---

## 1. 프로젝트 개요

- **대상**: 초등 고학년(10~13세)
- **형태**: 순수 HTML/CSS/바닐라 JS 3파일 + JSON 데이터 2개 (React 아님,
  빌드 도구 없음, GitHub Pages 같은 정적 호스팅에 그대로 올리는 구조)
- **핵심 기능**: ① 곤충 도감(28개 목, 43종) ② 즐겨찾기(최대 10종,
  기기 로컬 저장) ③ 환경 카드 + 공격/방어 동시선택 턴제 대결
  (1인용/2인용) ④ 비밀번호로 잠긴 관리자 페이지(GitHub 자동 커밋)

## 2. 파일 구조

```
index.html            아이들이 쓰는 실제 서비스 (도감 + 즐겨찾기 + 대결)
admin.html             곤충 데이터 편집 (비밀번호 필요)
admin-settings.html    GitHub 연결 + 관리자 비밀번호 변경 (비밀번호 필요)
insects.json            곤충 데이터 전체 (수정 시 이 파일만 건드리면 됨)
admin-config.json      관리자 비밀번호 해시 1줄짜리 파일
images/                 (선택) 사진을 직접 업로드할 때 쓰는 폴더
README.md               배포/사용 안내 (사용자용)
PROJECT_NOTES.md         이 문서 (개발자용)
```

세 HTML 파일은 서로 완전히 독립적인 `<script>`를 갖고 있어요(공유 JS
파일 없음). 로직이 겹치는 부분(예: 곤충 목 목록, GitHub API 호출 함수)은
**의도적으로 각 파일에 복사**해뒀어요 — 파일 하나만 열어도 이해/수정이
되게 하려는 선택이었어요. 즉, 예를 들어 "곤충 목(order) 종류"를
추가하면 `index.html`의 `ORDER_INFO`와 `admin.html`의 `ORDER_OPTIONS`
**둘 다** 고쳐야 해요.

## 3. 데이터 구조 (insects.json)

최상위 키:
- `_readme`, `_template` : 앱/관리자 페이지 모두 무시하는 안내용 필드
- `insects` : 배열. 아래는 항목 하나의 스키마

```jsonc
{
  "id": "beetle1",              // 영문 고유 id
  "name": "장수풍뎅이",
  "order": "coleoptera",        // ORDER_INFO / ORDER_OPTIONS 키 중 하나
  "shape": "beetle",             // 사진 없을 때 쓰는 SVG 그림 종류
  "photoUrl": "https://...jpg?width=300",  // null이면 SVG로 대체
  "hab": ["숲", "나무위"],        // 1~2개, HAB_OPTIONS 중에서
  "time": "밤",                   // 낮/밤/새벽/무관
  "weakWeather": "비",            // (선택) 이 날씨일 때 능력치 페널티
  "stats": { "hp":153, "atk":85, "def":70, "spd":40 },
  "features": "특징 설명 텍스트",
  "attack": "공격 방법 설명 텍스트",
  "skills": [
    { "name":"뿔 들이받기", "power":34, "type":"attack", "desc":"..." },
    { "name":"뒤집어 던지기", "power":44, "type":"attack", "desc":"..." },
    { "name":"몸통 밀어붙이기", "power":26, "type":"defense", "desc":"..." }
  ]
}
```

`skills`는 보통 3개(공격 2 + 방어 1이 많지만 정해진 규칙은 아님).
`skill.type`은 `"attack"` 또는 `"defense"`만 가능. `heal`,
`selfDamage` 필드를 스킬에 추가하면 특수 효과(회복/자해)가 붙어요
(예: 꿀벌의 필살기는 selfDamage:10).

**그림 모양(shape) 종류**: beetle, butterfly, bee, dragonfly, hopper,
truebug, fly, mantis, roach, stick, earwig (index.html의 `glyphSVG()`
함수에 하드코딩된 SVG path들. 새 모양을 추가하려면 이 함수에 case를
하나 더 넣어야 함)

## 4. 분류(목) 체계 — 28개

국립생물자원관 · 한국곤충명집(2022) 기준 곤충강 전체 목. 매미목은
분류학적으로 노린재목에 흡수됐지만, 아이들이 이해하기 쉽도록
**"매미노린재목"**이라는 이름을 그대로 유지하기로 했음(의도적 선택).

`index.html`의 `ORDER_INFO` 객체와 `admin.html`의 `ORDER_OPTIONS`
객체에 각각 28개 키가 정의돼 있음 (id → {name, color}). 현재 43종은
이 중 19개 목에만 분포돼 있고, 나머지 9개 목(예: 흰개미붙이목,
민벌레목, 부채벌레목 등 희귀 목)은 곤충이 아직 없음 — 관리자 페이지에서
추가 가능하도록 드롭다운에만 미리 준비해둔 상태.

## 5. 도감 & 즐겨찾기

- 도감/대결 곤충 선택 화면은 **사진 없이 목별로 묶은 텍스트 목록**으로
  구현(태블릿 로딩 속도 문제 때문에 사진은 상세보기 모달에서만 로드).
- 즐겨찾기: `localStorage` 키 `insectFavorites`에 id 배열로 저장,
  최대 10개(11번째부터는 안내 토스트만 뜨고 저장 안 됨). 기기별로
  따로 저장되고 다른 사람과 공유되지 않음.
- 사진은 위키미디어 커먼즈 URL 뒤에 `?width=300`을 붙여 축소본을
  받아오게 해서 로딩 용량을 크게 줄임 (Special:FilePath가 width
  파라미터를 지원).

## 6. 대결 시스템 상세 로직

모든 함수는 `index.html`의 `/* ============ 배틀 로직 ============ */`
구역에 있음.

### 6.1 환경 카드 (`ENV_CARDS`)
서식지(hab) × 시간대(time) × 날씨(weather) 조합 8장 고정. 대결 시작 전
랜덤으로 1장 뽑음 (같은 카드가 다시 나올 수 있음, 완전 독립 랜덤).

### 6.2 환경 보정 (`envModifiers(insect, env)`)
- 서식지 일치 → 공격력×1.2, 방어력×1.15
- 시간대 일치 → 속도×1.25
- `weakWeather` 일치 → 공격/방어/속도 ×0.85 (페널티)

### 6.3 데미지 공식 (`calcDamage`)
```
base   = 기술위력×0.75 + 공격자.공격력×0.35
defVal = 방어자.방어력 × defMult × 0.4
floor  = max(6, 방어자.최대체력×0.05)      // 최소 데미지
raw    = max(floor, base - defVal) × atkMult × (0.9~1.1 랜덤)
cap    = 방어자.최대체력 × 0.33            // ★ 한 방 최대 데미지 상한(즉사 방지)
결과   = min(raw, cap)
```
`cap`(33% 상한)이 밸런스의 핵심 장치. 이게 없으면 스탯 차이가 큰
매치업에서 항상 강자가 1~2방에 끝내버려서 확률 보정이 무의미해짐
(과거 시뮬레이션에서 확인됨 — 아래 6.12 참고).

### 6.4 명중 판정 (`computeAttackRoll`) — miss / graze / hit
```
missChance = 0.06 + 날씨기본값×(자기서식지=env.hab ? 0.4 : 1)
  + (약한날씨 일치 시 +0.10)
  + (활동시간대 일치 시 -0.03)
  + (내 종합전투력 > 상대×1.12 → +0.13, "방심")
  + (이 공격기가 '정밀' 특성이면 -0.07)
  → 0.02~0.40 사이로 clamp

grazeChance = 0.12 + 날씨기본값×0.6×(자기서식지 일치 시 0.5)
  + (정밀 특성이면 -0.05)
  → 0.03~0.30 사이로 clamp

날씨 기본 불안정도(WEATHER_MISS_BASE):
  맑음 0.04 / 흐림 0.06 / 무더위 0.08 / 비 0.10 / 바람 0.12
```
graze(스치듯 맞음)면 최종 데미지 ×0.5.

### 6.5 크리티컬 (`rollCrit`)
```
chance = 0.15
  + (활동시간대 일치 시 +0.08)
  + (내 종합전투력 < 상대×0.92 → +0.28, "언더독 보너스")
  → 최대 0.55로 clamp
크리티컬이면 데미지 ×1.5
```

### 6.6 회피 — 방어 기술을 냈을 때만 (`rollDodge`)
```
chance = 0.30
  + (활동시간대 일치 시 +0.15)
  + (이번 라운드 내 속도가 상대보다 빠르면 +0.10)
  + (이 방어기가 '회피형' 특성이면 +0.12)
  → 최대 0.75로 clamp
회피 성공 → 데미지 0
회피 실패 → 그래도 데미지의 65% 경감 (35%만 적용)
```

### 6.7 종합전투력 (`powerScore`) — 언더독 판단 기준
```
powerScore = 체력×0.5 + 공격력×1.0 + 방어력×0.5 + 속도×0.3
```

### 6.8 기적의 생존 (`applyDamageWithMiracle`)
상대보다 종합전투력이 0.7배 미만으로 훨씬 약한 쪽이, 이번 공격으로
체력이 0 이하가 될 뻔하면 **대결당 딱 한 번** 체력 1로 버팀
(`playerMiracleUsed` / `enemyMiracleUsed` 플래그로 1회 제한).

### 6.9 연속 공격 피로 (`fatigueFrac`, `applyFatigue`)
같은 쪽이 공격 기술을 연달아 낼 때마다 스트릭 카운트
(`playerAtkStreak`/`enemyAtkStreak`)이 오르고, 방어를 내면 0으로
초기화. 페널티는 **자기 최대체력 비율**로 자기 자신에게 적용(죽지는
않음, `Math.max(1, ...)`):
```
1회: 0%   2회: 3%   3회: 6%   4회 이상: 10%
```

### 6.10 공격/방어기 자동 차등화 — '정밀' / '회피' / '회복'
**데이터 파일은 건드리지 않고, 같은 곤충의 기술끼리 위력(power) 숫자를
비교해서 코드에서 자동으로 특성을 부여**하는 방식 (`attackVariant`,
`defenseVariant`, `effectiveDefenseHeal`, `skillTraitLabel`):
- 공격기가 2개 이상이고 위력이 서로 다르면 → **낮은 위력 쪽**이 `정밀`
  (명중 판정 보너스, 위 6.4 참고)
- 방어기가 2개 이상이면 → **위력이 더 높은 쪽**이 `회피`(회피 확률
  +12%p), **더 낮은 쪽**이 `회복`(맞든 안 맞든 상대 최대체력의 10%
  자동 회복, 최소 4)
- 기술이 하나뿐이면 변화 없음(`normal`)
- 데이터에 이미 `heal` 필드가 명시된 방어기(예: 진딧물의 "빠르게
  번식하기")는 그 값을 그대로 우선 사용 (`effectiveDefenseHeal`에서
  `skill.heal`이 있으면 그 값 우선)
- 버튼에는 `skillTraitLabel()`이 반환하는 단어(정밀/회피/회복)를
  기술 이름 옆에 작은 태그로 표시 (`.trait-tag` CSS 클래스)

### 6.11 턴 진행 방식
- **동시 선택(블라인드)**: 매 라운드 양쪽이 공격/방어 기술을 하나씩
  고름. 상대가 뭘 골랐는지는 공개 전까지 안 보임.
- **1인용(pvc)**: 플레이어가 고르면 컴퓨터가 0.7초 "생각하는 척"
  딜레이 후 자동 선택(`pickCpuSkill` — 자기 체력이 35% 미만이면 50%
  확률로 방어기 우선 고려).
- **2인용(pvp, 같은 기기)**: 플레이어1이 고르면 "플레이어2에게
  넘겨주세요" 안내 화면(`handoff`)이 뜬 뒤 플레이어2가 고름
  (엿보기 방지용 전환 화면일 뿐, 실제 기술적 차단은 아님).
- **양쪽 다 고르면 자동으로 진행되지 않고**, "⚔️ 결과 공개하기!"
  버튼을 눌러야 `resolveRound()`가 실행됨 (사용자 요청으로 자동 진행
  제거함).
- 결과는 화면 오른쪽 아래 "📖 규칙" 버튼(팝업)에서 전체 규칙을 다시
  볼 수 있음 (`renderRulesModal`).
- 왼쪽 아래 "✕ 나가기" 버튼으로 언제든 대결을 취소하고 도감 홈으로
  이동 가능.

### 6.12 밸런스 시뮬레이션 결과 (참고용 기록)
Node.js로 배틀 로직을 그대로 복제한 시뮬레이터(`/tmp/sim2.js`, 대화
중 임시로 작성해 검증만 하고 저장은 안 해둠 — 필요하면 이 문서의 공식을
참고해 다시 만들 것)로 8,000~15,000회씩 반복 검증한 결과:

| 지표 | 최종 값 |
|---|---|
| 전체 무작위 매치업, 약한 쪽 승률 | **약 24.4%** (목표 24~27% 달성) |
| 최약체(진딧물) vs 최강체(장수풍뎅이) | 0% → **약 3%** |
| 하위 30% vs 상위 30% | 약 10.5% |
| 평균 라운드 수 | 약 17~18 (양쪽 합산, 즉 각자 8~9턴) |

즉사 방지 상한(cap=33%)을 넣기 전에는 위 승률이 전부 0%에 가까웠음 —
확률 보정만으로는 "1~2방에 죽는" 구조를 못 뒤집었기 때문. **이 cap이
가장 중요한 레버**라는 걸 기억해둘 것. 승률을 더 올리고 싶으면
`cap`(현재 0.33)을 낮추거나 `underdogCritBonus`(0.28)를 올리면 됨 —
단, 대결이 길어지는 트레이드오프가 있음.

### 6.13 대결 화면 레이아웃 (전체화면 모드)
대결 탭에서는 `renderHeader()` 대신 작은 `.battle-top-bar`만 표시하고
하단 `<footer>`도 생략함(세로 공간 확보). 환경카드·체력박스·간격을
압축하고 `.battle-log`는 `min(52vh, 480px)`로 크게 키움. 각 단계
컨테이너는 `wrap battle-wrap` 클래스를 함께 써서 하단 플로팅 버튼
(나가기/규칙)과 안 겹치게 여백을 확보함.

### 6.14 사운드 (파일 없이 Web Audio API로 직접 합성)
`SoundEngine`(효과음, `index.html` 상단)과 `BgMusic`(배경음, 두 음을
번갈아 재생하는 긴장감 있는 루프) 객체가 있음. 음원 파일은 전혀 안 씀
— 오실레이터로 그때그때 소리를 만들어냄. `SoundEngine.play(이름)`으로
재생, 프리셋: `click, tick, hit, crit, miss, dodge, heal, fatigue,
miracle, win, draw, reveal, start, neutral`. 로그 한 줄 한 줄에
`L(텍스트, 사운드이름)` 헬퍼로 사운드를 붙여서 `resolveRound()`가
순서대로 공개할 때(`revealLogQueue`) 그 줄에 맞는 소리가 남.

`BgMusic.start()`는 `startBattle()`에서, `BgMusic.stop()`은 나가기/
처음부터/재대결/다른 탭으로 이동 시 호출됨. 브라우저 자동재생 정책
때문에 반드시 사용자의 클릭(제스처) 이후에만 소리가 남 —
`SoundEngine.ensureCtx()`가 AudioContext를 지연 생성/resume함.

**소리를 더 다양하게/고급스럽게 바꾸고 싶다면** `SoundEngine`의
`presets` 객체 안 각 함수(`tone()` 호출 조합)를 수정하면 됨. 진짜
음원 파일(mp3 등)을 쓰고 싶다면 `<audio>` 태그 + 저작권 없는 파일을
따로 준비해 붙이는 방식으로 바꿔야 함(현재는 그런 파일이 전혀 없음).

### 6.15 결과 순차 공개 (`revealLogQueue`)
`resolveRound()`는 이제 로그를 한 번에 합치지 않고, 계산이 끝난 로그
배열(`ctx.log`, 각 항목은 `{text, sound}`)을 `revealLogQueue()`에
넘김. 이 함수가 0.55초 간격으로 한 줄씩 `b.log`에 추가 + 렌더링 +
효과음 재생을 반복하다가 끝나면 `onDone` 콜백(다음 라운드 준비 또는
결과 화면 전환)을 실행함. **공개 중에는 `b.busy=true`라서 "결과
공개하기" 버튼이 다시 안 뜨고 "⚔️ 공개하는 중..."만 보임** —
`resolveRound()` 맨 앞의 `if (b.busy) return;` 가드와 함께 중복 클릭
버그를 막아줌. 한 줄 표시 간격(현재 550ms)을 바꾸려면
`revealLogQueue` 안의 `setTimeout(step, 550)` 숫자만 고치면 됨.

### 6.16 음성 안내 (`announce`, Web Speech API)
브라우저 내장 음성합성(`speechSynthesis`)을 그대로 사용 — 별도 파일
없음. `maybeAnnounceTurn()`이 라운드가 새로 시작될 때(`p1-choose`
진입 시, `p2-choose` 진입 시) 호출됨. 1인용 모드에서는 "기술을
골라보세요", 2인용 모드에서는 "플레이어 1/2, 기술을 고르세요"라고
말함. **기기/브라우저마다 한국어 음성 품질과 지원 여부가 다름** —
지원 안 하면 조용히 무시되도록 try/catch로 감싸둠.



### 7.1 비밀번호 잠금
- `admin.html`, `admin-settings.html` 둘 다 부팅 시
  `admin-config.json`을 fetch해서 `passwordHash`를 읽어옴 (실패하면
  코드에 박힌 기본 해시로 폴백 — insect1234의 SHA-256).
- 입력한 비�말번호를 `crypto.subtle.digest('SHA-256', ...)`로 해시화해
  비교. 맞으면 `sessionStorage.admin_unlocked='1'`로 표시(탭 닫으면
  풀림).
- **클라이언트 잠금이라 완전한 보안은 아님** — 소스를 볼 수 있는
  사람은 우회 가능. "실수로 들어오는 것 방지" 용도로만 생각할 것.

### 7.2 admin.html 구조
3개 박스로 재구성됨(요청에 따라):
1. **기본 정보**: 이름/id/분류(목)/그림모양/서식지/시간대/약한날씨/특징
2. **사진**: URL 붙여넣기 또는 파일 업로드(탭 전환)
3. **⚔️ 배틀 데이터**: 능력치 4개, 공격 방법, 기술 3개(이름/위력/
   종류(공격·방어)/설명)

왼쪽 사이드바에 곤충 목록(검색 가능), "+ 새 곤충 추가" 버튼. 하단
저장바에 "GitHub에 저장"(연결 필요) / "JSON만 다운로드"(수동 백업).
우측 상단에 GitHub 연결 상태 칩 + "⚙️ 설정" 링크 + "🏠 홈으로" 링크.
760px 이하 화면에서 자동으로 세로 스택 레이아웃으로 전환(반응형).

### 7.3 admin-settings.html
GitHub 연결(owner/repo/branch/token, `localStorage`에 저장)과
비밀번호 변경을 여기로 분리함(요청: "곤충마다 필요한 게 아니면 별도
페이지로"). 비밀번호 변경은 GitHub 연결이 되어 있어야만 동작
(새 해시를 계산해 `admin-config.json`에 커밋).

### 7.4 GitHub API 연동 방식 (`githubGetSha`, `githubPutFile`)
`https://api.github.com/repos/{owner}/{repo}/contents/{path}` 를
GET(sha 조회, 없으면 null) → PUT(내용 base64 인코딩 + sha 포함해 커밋)
하는 단순한 REST 호출. 토큰은 `Authorization: token {token}` 헤더로
전달, 브라우저에만 저장되고 서버로 전송 안 됨. 한글 텍스트는
`btoa(unescape(encodeURIComponent(str)))`로 UTF-8 안전하게 base64
인코딩.

## 8. 디자인 시스템

- **폰트**: 제목/버튼/숫자는 `Jua`(동글동글한 느낌), 본문은
  `Gowun Dodum`. Google Fonts `@import`로 로드.
- **색상 변수**(`:root` CSS 커스텀 프로퍼티):
  `--cream`(배경) `--leaf`(초록, 도감/1인용) `--coral`(빨강,
  대결/2인용) `--sun`(노랑) `--sky`(파랑) `--ink`(글자)
- **버튼 스타일**: 입체감 있는 "눌리는 버튼"(듀오링고 스타일) —
  `box-shadow: 0 Npx 0 진한색` + `:active`에서 `translateY`로 눌리는
  느낌.
- **곤충 그림**: 사진이 없을 때 표시되는 손그림풍 SVG (`glyphSVG`
  함수, 11가지 모양, `currentColor` 대신 목(order)별 지정 색 사용).

## 9. 수정 가이드 — 자주 바꿀 만한 것들

| 하고 싶은 것 | 어디를 고치나 |
|---|---|
| 곤충 정보/사진 수정, 추가 | `admin.html`로 편집 (또는 `insects.json` 직접 수정) |
| 새 분류(목) 추가 | `index.html`의 `ORDER_INFO` **+** `admin.html`의 `ORDER_OPTIONS` 둘 다 |
| 새 그림 모양(shape) 추가 | `index.html`의 `glyphSVG()` 함수에 case 추가 |
| 즐겨찾기 최대 개수(현재 10) | `index.html`에서 `FAV_KEY` 근처, 즐겨찾기 추가 로직의 길이 체크 부분 |
| 대결 밸런스(승률) 조정 | `index.html`의 `calcDamage`(cap/floor), `computeAttackRoll`, `rollCrit`, `rollDodge` 상수들 — 위 6.12 표 참고 |
| 환경 카드 종류/조합 | `index.html`의 `ENV_CARDS` 배열 |
| 관리자 비밀번호 | `admin-settings.html`에서 직접 변경(권장) 또는 `admin-config.json`의 해시 직접 교체 |
| 색상/폰트 등 디자인 | 각 HTML 파일 상단 `<style>`의 `:root` 변수 |
| 사진 로딩 속도 | 새 사진 URL 끝에 `?width=300` 붙이기 (위키미디어 커먼즈 기준) |

## 10. 알려진 제약사항 (의도적으로 안 한 것들)

- 관리자 비밀번호는 **진짜 보안이 아님** (정적 사이트 특성상 한계,
  README/설정 페이지에도 명시해둠)
- 즐겨찾기는 **기기/브라우저별로 따로 저장**됨, 여러 기기 동기화 없음
- GitHub Pages를 직접 열면 되지만, **로컬에서 파일 더블클릭으로
  열면 fetch가 막혀서** JSON을 못 불러올 수 있음(로컬 서버 필요)
- 사진은 외부(위키미디어) 링크 참조 방식이 기본값 — 원본 사이트가
  파일을 옮기거나 지우면 깨질 수 있음(그럴 땐 자동으로 SVG 그림으로
  대체되서 화면이 아예 깨지진 않음)
- 매우 희귀한 목(흰개미붙이목, 민벌레목, 대벌레붙이목 등)에는 아직
  대표 곤충이 없음

## 11. 이번 대화에서 진행된 주요 변경 이력 (요약)

1. React 아티팩트 → HTML 단일 파일(이후 다중 파일) 구조로 전환
2. 어린이 친화적 UI(밝은 색감, Jua 폰트, 입체 버튼)로 리뉴얼
3. 도감 28종 → 43종으로 확장, 실제 위키미디어 사진 링크 부착
4. 관리자 페이지: 비밀번호 잠금, GitHub 자동 커밋, 사진 URL/업로드
   겸용, 이후 GitHub 설정을 별도 페이지(admin-settings.html)로 분리
5. 파일 구조를 `data/` 하위 폴더 → 저장소 루트 평면 구조로 변경
6. 배틀 시스템을 "속도 기반 순차 턴제" → "공격/방어 동시선택 + 명중
   판정(빗나감/스치듯/제대로) + 크리티컬/회피 + 환경(서식지·시간대·
   날씨) 반영 + 언더독 보정 + 기적의 생존 + 연속공격 피로 + 정밀/회피/
   회복 자동 차등화"로 크게 발전
7. 도감/대결 선택 화면에서 사진 제거(목별 텍스트 목록), 즐겨찾기
   기능(최대 10종, localStorage) 추가
8. 대결 화면에 규칙 팝업, 결과 공개 버튼(수동 진행), 나가기 버튼 추가
9. 밸런스를 "약한 쪽도 약 24~27% 확률로 이길 수 있게" 시뮬레이션
   기반으로 재조정 (핵심: 1회 공격 데미지 상한 33% 도입)
10. 대결 화면을 전체화면처럼(제목/메뉴 숨김) 재구성, 로그창 확대,
    결과를 한 줄씩 순차 공개 + 상황별 효과음, Web Audio 기반 배경음악,
    Web Speech API 음성 안내, 규칙 팝업 문구를 태그 기반 설명으로 수정
    ("언더독 보정"→"언더독의 투지")
