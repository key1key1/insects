# images 폴더

곤충 사진 파일을 이 폴더에 넣어주세요.

- 파일명은 `admin.html`에서 자동으로 제안하는 `곤충id.jpg` (또는 원본 확장자)
  형태를 그대로 쓰는 걸 추천해요.
- 사진이 없는 곤충은 `data/insects.json`의 `photoUrl`을 `null`로 두면
  자동으로 손그림풍 SVG 아이콘이 대신 표시됩니다.
- 저작권 문제가 없는 사진(직접 찍은 사진, 또는 위키미디어 커먼즈의
  CC0/CC-BY 라이선스 사진 등)만 사용해주세요.
